name: supabase_mocktail_testing
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.2

dev_dependencies:
  faker: 2.1.0
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0

flutter:

  uses-material-design: true
